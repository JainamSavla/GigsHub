name: CI/CD Pipeline

on:
  push:
    branches: [main, devops]
  pull_request:
    branches: [main]

env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

jobs:
  # Stage 1: Checkout and Tool Install
  setup:
    name: Setup and Check Changes
    runs-on: ubuntu-latest
    outputs:
      client-changed: ${{ steps.changes.outputs.client }}
      api-changed: ${{ steps.changes.outputs.api }}
    steps:
      - name: Checkout SCM
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check Changes
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            client:
              - 'client/**'
            api:
              - 'api/**'

      - name: Tool Install
        run: |
          echo "Installing required tools..."
          docker --version
          node --version
          npm --version

  # Stage 2: Build and Scan Client
  client-pipeline:
    name: Client Build & Scan
    runs-on: ubuntu-latest
    needs: setup
    if: needs.setup.outputs.client-changed == 'true' || github.ref == 'refs/heads/devops'
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install Dependencies
        working-directory: ./client
        run: npm ci --legacy-peer-deps

      - name: Run Lint
        working-directory: ./client
        run: npm run lint

      - name: Run Prettier Check
        working-directory: ./client
        run: npm run format:check

      - name: SonarQube Scan - Client
        uses: SonarSource/sonarqube-scan-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          projectBaseDir: ./client
          args: >
            -Dsonar.organization=jainamsavla
            -Dsonar.projectKey=jainamsavla_gigshub_client
            -Dsonar.sources=src
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info

      - name: Build Docker Image - Client
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/gigshub-client:${{ github.sha }} \
                       -t ${{ secrets.DOCKER_USERNAME }}/gigshub-client:latest \
                       ./client

      - name: Trivy Security Scan - Client (SARIF)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/gigshub-client:latest
          format: 'sarif'
          output: 'trivy-client-results.sarif'

      - name: Trivy Security Scan - Client (HTML Report)
        uses: aquasecurity/trivy-action@master
        if: always()
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/gigshub-client:latest
          format: 'html'
          output: 'trivy-client-report.html'

      - name: Trivy Security Scan - Client (JSON Report)
        uses: aquasecurity/trivy-action@master
        if: always()
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/gigshub-client:latest
          format: 'json'
          output: 'trivy-client-report.json'

      - name: Upload Trivy Results to Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-client-results.sarif'
          category: 'trivy-client'

      - name: Upload Trivy Reports as Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-client-reports
          path: |
            trivy-client-report.html
            trivy-client-report.json
          retention-days: 30

      - name: Push to DockerHub - Client
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/devops'
        run: |
          echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker push ${{ secrets.DOCKER_USERNAME }}/gigshub-client:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/gigshub-client:latest
          docker logout

  # Stage 3: Build and Scan API
  api-pipeline:
    name: API Build & Scan
    runs-on: ubuntu-latest
    needs: setup
    if: needs.setup.outputs.api-changed == 'true' || github.ref == 'refs/heads/devops'
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: api/package-lock.json

      - name: Install Dependencies
        working-directory: ./api
        run: npm ci

      - name: SonarQube Scan - API
        uses: SonarSource/sonarqube-scan-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          projectBaseDir: ./api
          args: >
            -Dsonar.organization=jainamsavla
            -Dsonar.projectKey=jainamsavla_gigshub_api
            -Dsonar.sources=.
            -Dsonar.exclusions=node_modules/**
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info

      - name: Build Docker Image - API
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/gigshub-api:${{ github.sha }} \
                       -t ${{ secrets.DOCKER_USERNAME }}/gigshub-api:latest \
                       ./api

      - name: Trivy Security Scan - API (SARIF)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/gigshub-api:latest
          format: 'sarif'
          output: 'trivy-api-results.sarif'

      - name: Trivy Security Scan - API (HTML Report)
        uses: aquasecurity/trivy-action@master
        if: always()
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/gigshub-api:latest
          format: 'html'
          output: 'trivy-api-report.html'

      - name: Trivy Security Scan - API (JSON Report)
        uses: aquasecurity/trivy-action@master
        if: always()
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/gigshub-api:latest
          format: 'json'
          output: 'trivy-api-report.json'

      - name: Upload Trivy Results to Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-api-results.sarif'
          category: 'trivy-api'

      - name: Upload Trivy Reports as Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-api-reports
          path: |
            trivy-api-report.html
            trivy-api-report.json
          retention-days: 30

      - name: Push to DockerHub - API
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/devops'
        run: |
          echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker push ${{ secrets.DOCKER_USERNAME }}/gigshub-api:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/gigshub-api:latest
          docker logout

  # Stage 4: Post Actions
  post-actions:
    name: Post Actions
    runs-on: ubuntu-latest
    needs: [client-pipeline, api-pipeline]
    if: always()
    steps:
      - name: Pipeline Summary
        run: |
          echo "## Pipeline Execution Summary 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Client Pipeline: ${{ needs.client-pipeline.result }}" >> $GITHUB_STEP_SUMMARY
          echo "✅ API Pipeline: ${{ needs.api-pipeline.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Deliverables" >> $GITHUB_STEP_SUMMARY
          echo "🐳 Docker images pushed to DockerHub" >> $GITHUB_STEP_SUMMARY
          echo "🔒 Security scans completed with Trivy" >> $GITHUB_STEP_SUMMARY
          echo "📊 Code quality analyzed with SonarCloud" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📥 Available Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "- **trivy-client-reports**: Security scan reports for Client (HTML & JSON)" >> $GITHUB_STEP_SUMMARY
          echo "- **trivy-api-reports**: Security scan reports for API (HTML & JSON)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "💡 Download the artifacts below to view detailed security reports" >> $GITHUB_STEP_SUMMARY
